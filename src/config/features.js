/**
 * Optional product flags. Dummy payments satisfy coursework without a real gateway.
 * For PayPal PayMongo etc., swap the saga implementation and enable the flag when ready.
 */
export const PAYMENTS_ENABLED = false;
